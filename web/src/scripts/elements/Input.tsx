import styled from "styled-components";

export const Input = styled.input`
  display: block;
  border-radius: 15px;
  border-color: #AAA;
  outline: none;

  &:hover, &:focus {
    box-shadow: 2px 2px 2px #888;
  }

`;
